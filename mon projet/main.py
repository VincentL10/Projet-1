 #!/usr/bin/python
 #coding:utf-8

from class import exemple

from cryptography.fernet import fernet
key = Fernet.generate_key()
f = Fernet(key)

current_disk = os.environ["SystemeDrive"]
PATH="{}\\USERS\\".format(current_disk)

# L'importation de l'ensemble des éléments du paquet tkinter :
from tkinter import*
fenetre = Tk()
fenetre.mainloop()
